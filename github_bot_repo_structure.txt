.github/workflows/label.yml
```yaml
name: Label new issues

on:
  issues:
    types: [opened]

jobs:
  add-label:
    runs-on: ubuntu-latest
    steps:
      - name: Add triage label
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: triage
```

.github/workflows/auto-comment.yml
```yaml
name: Auto Reply to New Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  auto-reply:
    runs-on: ubuntu-latest
    steps:
      - name: Post auto-reply comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: |
            👋 Automated Notice — GitHub Interact Team

            This issue has been flagged for closure due to inactivity.
            Our support team at **GitHub Interact** will guide you step by step until the issue is fully resolved.

            For faster help, or if you want priority troubleshooting, please contact us:
            - Email: GitHub.interact@gmail.com
            - Web Support: https://git-cryptoresolvehub.web.app/

            We will triage this issue shortly.

            — GitHub Interact Team 🚀
```

.github/workflows/smart-reply.yml
```yaml
name: Smart Auto Reply to Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  smart-reply:
    runs-on: ubuntu-latest
    steps:
      - name: Determine reply
        id: detect
        run: |
          issue_body="${{ github.event.issue.body }}"

          if echo "$issue_body" | grep -qi "wallet"; then
            echo "message=Thank you for reaching out regarding your **wallet issue**. Our support team will assist you shortly." >> $GITHUB_OUTPUT

          elif echo "$issue_body" | grep -qi "reset\|password"; then
            echo "message=It looks like you're having a **password/reset-related issue**. Please follow our recovery steps or contact support for immediate help." >> $GITHUB_OUTPUT

          elif echo "$issue_body" | grep -qi "bug"; then
            echo "message=Thanks for reporting a **bug**. Our technical team will review and follow-up." >> $GITHUB_OUTPUT

          else
            echo "message=Thank you for submitting your issue. Our support team will triage it shortly." >> $GITHUB_OUTPUT

      - name: Post comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: |
            👋 Automated Support Response

            ${{ steps.detect.outputs.message }}

            **For faster help**, contact:
            - Email: GitHub.interact@gmail.com
            - Web Support: https://git-cryptoresolvehub.web.app/

            — GitHub Interact Team 🚀
```

.github/workflows/close-stale.yml
```yaml
name: Close stale issues

on:
  schedule:
    - cron: "0 0 * * *"   # Runs every day

permissions:
  issues: write

jobs:
  close-stale:
    runs-on: ubuntu-latest
    steps:
      - name: Close inactive issues
        uses: actions/stale@v9
        with:
          days-before-stale: 5
          days-before-close: 7
          stale-issue-message: >
            This issue has been marked as stale due to inactivity.
            Please reply if you need further help.
          close-issue-message: >
            Closing this issue due to prolonged inactivity.
            You may reopen if needed.
```

.github/workflows/assign.yml
```yaml
name: Auto Assign Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: pozil/auto-assign-issue@v2
        with:
          assignees: snykart
          numOfAssignee: 1
```

.github/ISSUE_TEMPLATE/support.yml
```yaml
---
name: Support request / Bug report
about: Use this template to report a support request or bug. Our team will triage it.
title: "[SUPPORT] "
labels: support
---

**Description**
Please describe the issue in detail.

**Steps to reproduce**
1. 
2. 
3. 

**Expected behavior**

**Screenshots / Links (if any)**

**Environment (browser, OS, etc.)**
```
