name: Auto Reply to New Issues

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  auto-reply:
    runs-on: ubuntu-latest
    steps:
      - name: Post auto-reply comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: |
            👋 **Automated Support Response**

            Thank you for submitting your issue. Our support team will review it shortly.

            For quick assistance, click here:
            [Open Live Chat Support](https://git-cryptoresolvehub.web.app/)

            Email: GitHub.interact@gmail.com

               — GitHub Interact Team 🚀


